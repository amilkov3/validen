package io.voklim

package object validen {

  val VParser = io.voklim.validen.parser.VParser

  type ShapelessValidator = validen.validator.ShapelessValidator
  type ProductIteratorValidator = validen.validator.ProductIteratorValidator
}
